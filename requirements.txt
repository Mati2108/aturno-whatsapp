# Dependencias directas, con versión fijada.
#
# Pinneadas a propósito: sin versión, lo que se despliega puede no ser lo
# que se probó. La primera vez que este archivo salió sin fijar, además le
# faltaban dos paquetes que existían solo en el entorno local y el deploy
# falló al arrancar con ModuleNotFoundError.

# Orquestación y LLM
langchain==1.3.15
langchain-core==1.5.5
langgraph==1.2.11
langchain-anthropic==1.5.6
langchain-openai==1.5.1
langchain-google-genai==4.3.4
langchain-ollama==1.1.0

# Persistencia del checkpointer
# psycopg[binary] y no psycopg pelado: la variante binaria trae libpq incluida.
# Sin el extra, psycopg busca libpq en el sistema — que existe en una Mac con
# Postgres instalado por brew, pero no en una imagen slim. El deploy falló con
# "no pq wrapper available" mientras en local andaba perfecto.
langgraph-checkpoint-postgres==3.1.2
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# RAG
langchain-chroma==1.1.0
chromadb==1.5.9
langchain-text-splitters==1.1.2
fastembed==0.8.0

# API y canal de WhatsApp
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32
twilio==9.11.0
httpx==0.28.1

# Validación y configuración
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
typing-extensions==4.16.0

# Observabilidad
arize-phoenix==20.2.1
openinference-instrumentation-langchain==0.1.70
